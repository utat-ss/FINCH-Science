Messy code pushed, because otherwise I will never push any code at all.

Most are notebooks.

`spectral_library.py` is a bunch of helper scripts. (Library as in python library.)

The libraries the usual:

- numpy
- sklearn
- matplotlib
- scipy